# ============================================
# CLIENT-SIDE ENVIRONMENT VARIABLES (VITE_)
# ============================================
# These are exposed to the browser (safe for public keys only)

# Supabase Configuration (Client)
# Get these values from your Supabase project settings
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# ============================================
# SERVER-SIDE ENVIRONMENT VARIABLES
# ============================================
# These are ONLY used in /api serverless functions (never exposed to browser)

# Supabase Configuration (Server)
# Use the same URL but with SERVICE ROLE KEY for server-side operations
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Resend API Key for Email Confirmations
# Get this from https://resend.com/api-keys
# IMPORTANT: This should ONLY be used server-side
RESEND_API_KEY=re_your-resend-key-here

# Security Configuration
# CORS allowed origin - your production domain
ALLOWED_ORIGIN=https://elitedetailing.com

# Node environment (development, production)
NODE_ENV=production

# ============================================
# INSTRUCTIONS
# ============================================
# 1. Copy this file to .env (don't commit .env to git)
# 2. Replace the placeholder values with your actual keys
# 3. For Vercel deployment, add ALL variables (both VITE_ and non-VITE_) to environment variables
# 4. Restart your development server for changes to take effect
# 5. See SECURITY-SETUP.md for detailed setup instructions
